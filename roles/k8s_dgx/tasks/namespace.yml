---
- name: Create workload namespaces
  kubernetes.core.k8s:
    state: present
    definition:
      apiVersion: v1
      kind: Namespace
      metadata:
        name: "{{ item }}"
  loop:
    - "{{ sglang_namespace }}"
    - "{{ vllm_namespace }}"
    - "{{ openwebui_namespace }}"
